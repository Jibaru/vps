# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=appdb

# Application Configuration
# DATABASE_URL is automatically constructed from the above variables
